# Run backend
cd api
python -m venv .venv   -> for the first time if you don't have any virtual .venv folder

.venv\Scripts\Activate.ps1 -> for activating the virutal .venv folder

.venv -> it is a folder which contains only packages, libraries and dependencies 